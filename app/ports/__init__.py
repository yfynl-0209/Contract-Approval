"""端口（Port）定义 —— 外部依赖的唯一抽象边界，只放 `Protocol` 与 DTO。

依赖方向：`api/` → `services/` → `ports/` ← `adapters/`。
业务层不得直接导入审批厂商 SDK、MinIO SDK、OCR SDK 或模型 SDK；
换厂商只改 Adapter，业务代码与测试都不受影响。

端口**按能力拆分**而不是合成一个大接口：真实审批系统常只授予读取权限，
把读写塞进同一个 Protocol 会迫使这类系统伪造一个永远返回空的评论查询能力 ——
比直接缺失更糟，因为它看起来是能用的。
"""
