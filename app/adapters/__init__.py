"""适配器（Adapter）—— 全项目唯一允许导入外部 SDK 的地方。

依赖方向：`api/` → `services/` → `ports/` ← `adapters/`。
本包是端口依赖约束的**唯一例外出口**。

适配器承担四件事：鉴权、分页与限流、厂商字段 → 内部 DTO 的映射、
HTTP 状态码 → 端口异常的转换。

最后一条最关键：业务层**永远不接触 HTTP 状态码**，否则每个服务方法里都会
散落 `if response.status_code == 404` 之类的判断。
"""
