# 合同审批审查

本上下文描述合同进入审批流程后，系统用于识别我方立场、核验合同证据并形成风险评价的统一业务语言。

## Language

**审批任务（Approval Task）**:
审批系统交给审查系统处理的一份合同审批事项，以审批编号作为业务唯一标识。
_Avoid_: 审查批次、合同文件

**我方（Our Party）**:
本次审查所代表的企业主体，其名称由审批系统或人工确认提供。
_Avoid_: 默认甲方、默认采购方

**合同标签（Contract Label）**:
我方在合同正文中的形式标签，例如甲方、乙方或其他方；它不等同于业务角色。
_Avoid_: 我方角色

**业务角色（Business Role）**:
我方在交易中的实际身份，例如采购方、销售方、客户或服务提供方。
_Avoid_: 甲方、乙方

**合同类型（Contract Type）**:
由审批流程选择并确认的合同业务分类，用于判断审查规则是否适用。
_Avoid_: 解析器猜测的文档类型

**权威审查上下文（Authoritative Review Context）**:
由审批系统或人工确认提供的我方、合同标签、业务角色和合同类型等业务事实。
_Avoid_: 解析结果、模型推断

**解析证据（Parsed Evidence）**:
从合同文件中提取的字段、原文片段、位置和识别质量，用于核验权威审查上下文和支持风险判断。
_Avoid_: 权威业务事实

**审查批次（Review Run）**:
针对一份确定的解析结果、权威审查上下文快照和规则集版本执行的一次完整规则评价。
_Avoid_: 审批任务、解析记录

**规则评价（Rule Evaluation）**:
一条规则在一个审查批次中的完整结论，结果为命中、未命中、不适用或需人工判断之一。
_Avoid_: 规则命中

**命中（Hit）**:
规则适用于当前合同，且风险条件已经得到足够证据支持。

**未命中（Not Hit）**:
规则适用于当前合同，但风险条件不成立。

**不适用（Not Applicable）**:
规则的适用条件明确不满足，因此不参与当前合同的风险判断。

**需人工判断（Needs Review）**:
规则可能适用，但因权威上下文缺失或冲突、证据不足或模型不可用而无法可靠得出结论。具体原因由稳定的原因码（Reason Code）表达，术语表不重复列举。

**结论完整性（Review Completeness）**:
一份审查结论的整体可信度，取值为完整或需人工判断。它与单条规则的"需人工判断"是两个层级，不可混用。
_Avoid_: 规则评价状态

**权威上下文状态（Context Status）**:
描述权威审查上下文是否完整可信，取值为完整、缺失、冲突或已人工确认，是回写门禁的输入。
_Avoid_: 解析状态

**立场确认（Context Confirmation）**:
人工对权威审查上下文（我方、合同标签、业务角色、合同类型）的承认，独立于对审查结果与回写正文的确认。
_Avoid_: 高风险确认

**结果确认（Result Confirmation）**:
人工对当前审查结果与回写正文的承认，独立于立场确认；回写正文一旦变更，该确认即失效。
_Avoid_: 立场确认

**审查结果（Review Result）**:
一个审查批次产出的一份可被人工确认、可被回写的结论。同一批次可以有多份结果，构成该批次内的版本链；只有最新版本才有资格回写。
_Avoid_: 审查批次（批次是执行，结果是产出）

**结果指纹（Result Fingerprint）**:
在一个批次内标识"这是同一份结果"的稳定值，用于重复保存时复用已有结果而不是新增版本。与内容是否相同无关。
_Avoid_: 回写正文摘要

**回写正文摘要（Content Digest）**:
回写正文的 SHA-256。它是人工确认所绑定的对象：确认记录下当时的摘要，此后的正文一旦变化，摘要不再相等。
_Avoid_: 结果指纹（指纹认记录，摘要认内容）

**确认有效性（Confirmation Validity）**:
"这份确认现在还算不算数"的**后端判据**，由三个条件同时成立才算有效：人工已确认、确认时的摘要与当前正文摘要相等、该版本仍是最新版本。前端不得自行比较两个摘要来推断它。
_Avoid_: 摘要相等

**回写意图（Writeback Intent）**:
"要往审批系统评论区写这条评论"这个业务事实。它与业务状态**同事务**落库，因此要么一起存在、要么一起不存在；它不等于评论已经发出。
_Avoid_: 已回写

**投递事件（Outbox Event）**:
待投递的外部调用记录，携带标识与内容摘要，由独立的投递进程负责真正送达；**不携带合同正文**。投递成功之前，回写意图一直处于待投递状态。
_Avoid_: 评论

**回写被拒（Writeback Denial）**:
门禁判定"这次不该写"（未确认、立场不可信、已有正文缺失等）。这是一个**业务结论**，没有发起外部调用，因此重试没有意义。
_Avoid_: 回写失败

**投递失败（Delivery Failure）**:
已经发起外部调用，但调用本身出错（超时、上游 5xx）。这是一个**系统故障**，重试有意义；且超时属于结果未知，必须先按幂等键查询再决定是否重发。
_Avoid_: 回写被拒（一个是判定不该写，一个是写了但没送达）

**回写尝试（Writeback Attempt）**:
一次具体的回写执行，带尝试序号。任务级的写入状态回答"这张单子写成了没有"，尝试级的原因回答"最近这一次为什么没成"；两者不可互相替代。
_Avoid_: 回写日志

**写入状态（Write Status）**:
一次写入走到了哪一步，取值为未写入、写入中、成功、失败四种。它与原因码是**正交**的两个维度：状态回答"走到哪一步"，原因回答"为什么没成功"。
_Avoid_: 把"被拒"记成失败（被拒时根本没有发起写入，状态是未写入）


**主体（Actor）**:
一次请求背后**是谁**，由身份提供方的凭据解析得出。它由**后端**从凭据解出，调用方没有提供它的入口。v1 不建用户表，角色来自身份提供方令牌里的声明。
_Avoid_: 调用方、用户名（那是一个字符串字段，主体是已认证的身份结构）

**权限（Permission）**:
后端能够判定的最小能力单位（如"保存结果"）。路由声明**权限**，不判断角色字符串——角色是身份提供方给的名字，权限才是本系统自己的决定。
_Avoid_: 角色

**角色声明（Role Claim）**:
令牌里原样的角色名。它是身份提供方说的话，本系统只负责把它映射成权限；原话保留下来，是为了在拒绝时能说清"我看到你带了什么"。
_Avoid_: 权限

**未识别角色（Unknown Role）**:
令牌里带了、但本系统不认识的角色名。它**不带来任何权限**，但**不导致拒绝服务**——身份提供方先上、本系统后跟时，报错会让所有人无法使用，而少给权限只让那个人看到 403。角色原话会出现在 403 消息里。
_Avoid_: 忽略、非法角色（它是"不认识"，不是"不合法"）

**身份配置错误（Auth Configuration Error）**:
运维把身份配错了（生产环境选了开发期身份来源、JWT 缺密钥或签发方）。它**不是**"来者身份不可信"，因此不能翻成 401：那会让每个调用方都去重新登录，而该做的事是改配置——**任何调用方都做不到**。它以 500 暴露，或在启动期直接让进程起不来。
_Avoid_: 认证失败

**认证失败（Authentication Failure）**:
没能确定"你是谁"。对应 401，正确处置是**换一份身份**。
_Avoid_: 授权失败

**授权失败（Authorization Failure）**:
已经知道"你是谁"，但你没有做这件事的权限。对应 403，正确处置是**换账号或补授权**；重试多少次都一样。
_Avoid_: 认证失败（两者的正确处理方式不同，合并后客户端必然有一半做错）
