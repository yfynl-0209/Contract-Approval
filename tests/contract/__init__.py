"""可复用的**跨实现合约测试**。

本目录存放"同一端口的不同实现都必须通过"的测试。

与 `tests/` 下其他文件的区别：

| | 普通测试 | 合约测试 |
| --- | --- | --- |
| 面向 | 某个具体实现 | 端口的**语义** |
| 新增实现时 | 重写一遍 | 继承后自动获得 |
| 回答的问题 | "它能跑吗" | "它能**替换**另一个实现吗" |

`Protocol` 的 `runtime_checkable` 只检查方法名，不校验签名、更不校验语义 ——
所以"可替换"这件事只能靠跑同一套测试来证明。
"""
